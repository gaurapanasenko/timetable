import os

BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

SECRET_KEY = ''

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': '', # add db name
        'USER': '', # add db user
        'PASSWORD': '', # add db user password
        'HOST': 'localhost',
        'PORT': '',
    }
}
